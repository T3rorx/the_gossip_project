<!DOCTYPE html>
<html lang="fr" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>The Gossip Project</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body class="text-center">
    <h1 class="display-6 fw-bold mb-3 card bg-body-tertiary shadow-sm rounded-3 p-4 mx-auto">Mon super site de gossip !</h1>
    <a class="btn btn-light" href="/gossips/new/">Crée un nouveau potin !</a>
    <div class="d-flex flex-column align-items-center gap-3 mt-4">
      <% gossips.each_with_index do |gossip, index| %> 
        <div class="card bg-body-tertiary shadow-sm rounded-3 p-4 w-100" style="max-width: 480px;">
          <a href="/gossips/<%=index + 1%>">Potin n°<%=index + 1%></a>
          <p class="mb-1">By: <%= gossip.author %></p>
        </div>
      <% end %>
    </div>
  </body>
</html>